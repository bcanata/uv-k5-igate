{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window: core APIs (events) only — app commands are not permission-gated",
  "windows": ["main"],
  "permissions": ["core:default"]
}
